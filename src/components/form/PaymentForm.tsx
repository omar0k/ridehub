import { trpc } from "@/app/_trpc/client";
import { FormInfoData } from "@/types";
import { UseFormReturn } from "react-hook-form";
import { Button } from "../ui/button";

const PaymentForm = ({ name }: { name: string }) => {
  name = "apple";
  return <div></div>;
};
export default PaymentForm;
